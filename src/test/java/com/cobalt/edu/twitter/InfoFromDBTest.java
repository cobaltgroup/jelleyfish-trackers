package com.cobalt.edu.twitter;

import java.util.ArrayList;

import org.junit.Assert;
import org.junit.Test;

import com.google.gson.Gson;

public class InfoFromDBTest {
		/*InfoFromDB databaseObj= new InfoFromDB();
		
		@Test
		public void testModelNamesNotNullNotEmpty(){
		ArrayList<String> chevyModelNames=databaseObj.getModelNames("Chevrolet");
		Assert.assertNotNull(chevyModelNames);
		Assert.assertNotEquals(chevyModelNames.size(), 0);
		}
		
		@Test
		public void testInsertCarInsertsCar(){
		databaseObj.insertCar("Volkswagon","vw");
		ArrayList<String> dbResults=databaseObj.getMakeNames();
		boolean includesVW=false;
		for (String i: dbResults){
			if(i.toLowerCase().equals("volkswagon"))
				includesVW=true;
		}
		Assert.assertTrue(includesVW);
		}
		
		@Test
		public void testDeleteCarDeletesCar(){
		databaseObj.deleteCar("BMW");
		ArrayList<String> dbResults2=databaseObj.getMakeNames();
		boolean noBMW=true;
		for (String i: dbResults2){
			if(i.toLowerCase().equals("bmw"))
				noBMW=false;
		}
		Assert.assertTrue(noBMW);
		}*/

}